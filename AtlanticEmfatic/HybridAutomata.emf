package Metamodel;

package HybridAutomata {
  class FiniteTransition {
    !unique !ordered attr String trigger;
    !unique !ordered attr String guard;
    !unique !ordered attr String action;
  }

  class FiniteState {
    !unique !ordered attr String name;
    !unique !ordered attr String position;
    !unique !ordered attr Boolean[1] isInit;
  }

}

package PrimitiveTypes {
  datatype String : "";

  datatype Boolean : "";

  datatype Integer : "";

}

